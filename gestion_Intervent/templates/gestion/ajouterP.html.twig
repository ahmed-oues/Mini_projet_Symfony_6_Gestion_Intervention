{% extends 'gestion-layout.html.twig' %}
{% block body %}
<h2 style=" font-family: Copperplate, Papyrus"> ➕ Ajouter un produit </h2>
{{form_start(f)}}
{{form_widget(f)}}
{{form_end(f)}}
 <a href="{{path('app_acceuil2')}}"><button style = "position:relative; left:70% " type="submit" class="btn btn-black"> retour</button></a>
{% endblock %}